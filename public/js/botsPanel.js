var botsPanel = new function() {
  var self = this;

  // Run on page load
  this.init = function() {
  };

  // Runs when panel is made active
  this.onActive = function() {
  };
}

// Init class
botsPanel.init();